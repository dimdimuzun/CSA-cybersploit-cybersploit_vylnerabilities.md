Поясню команду по частинах:

```
SHELL=/bin/bash script -q /dev/null
```

Це не одна команда, а **запуск програми `script` з перевизначенням змінної середовища `SHELL`**.

---

## 🔹 1. `SHELL=/bin/bash`

Це тимчасове встановлення змінної середовища **тільки для цієї команди**.

Воно означає:

> Запускати підоболонку `/bin/bash` замість стандартної shell користувача.

Команда `script` при старті запускає нову shell-сесію — і вона візьме саме `/bin/bash`.

Перевірка:

```bash
echo $SHELL
```

---

## 🔹 2. `script`

`script` — це утиліта для:
✅ запису всієї термінальної сесії (ввід, вивід, команди)
✅ логування або аудиту
✅ відтворення сесії пізніше

Фактично вона створює псевдо-термінал і запускає в ньому shell.

Типове використання:

```bash
script session.log
```

---

## 🔹 3. `-q` (quiet)

Параметр `-q` вимикає службові повідомлення:

Без `-q`:

```
Script started, file is ...
Script done.
```

З `-q` — нічого зайвого не виводиться.

Корисно для автоматизації та скриптів.

---

## 🔹 4. `/dev/null`

Це файл-поглинач:

> Усе, що туди записується — зникає.

Тут він використовується як файл логування для `script`.

Тобто:
📌 Сесія **не записується на диск взагалі**.

---

## ✅ Підсумок

Команда:

```
SHELL=/bin/bash script -q /dev/null
```

означає:

> 🔹 Запусти нову інтерактивну bash-сесію через утиліту `script`,
> 🔹 без службових повідомлень,
> 🔹 не зберігаючи лог (усе йде в /dev/null).

Фактично це:
✔️ запуск чистої shell-сесії
✔️ у псевдо-терміналі
✔️ без логування
✔️ часто використовується для коректної роботи програм, яким потрібен TTY (наприклад: sudo, ssh, screen, деякі експлойти або automation-скрипти).

---

## 🎯 Практичний приклад (для лабораторій / CTF)

У пентест- або CTF-середовищах таку команду часто застосовують, щоб:

* отримати **повноцінний TTY** після reverse shell;
* активувати коректну роботу:

  * `su`
  * `sudo`
  * `nano`, `top`
  * tab-completion
  * Ctrl+C / Ctrl+Z

Аналог:

```bash
python -c 'import pty; pty.spawn("/bin/bash")'
```

---

Якщо хочеш — можу показати:
✅ як перевірити, що тепер є TTY
✅ чим `script` відрізняється від `pty.spawn()`
✅ де ця команда корисна в DevOps / security лабораторіях
✅ типові помилки та обмеження
